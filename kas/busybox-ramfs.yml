# Busybox + Initramfs Image Configuration
#
# This file configures the build for:
# - Init System: busybox
# - Image Type: Initramfs (cpio)
#
# Usage:
#   kas build kas/v3-sort.yml:kas/busybox-ramfs.yml

header:
  version: 14

target:
  - deepx-image-busybox-init-initramfs

local_conf_header:
  init_system: |
    VIRTUAL-RUNTIME_init_manager = "busybox"

  image_type: |
    IMAGE_FSTYPES ?= "cpio.gz cpio.lz4"
    IMAGE_ROOTFS_EXTRA_SPACE = "0"
    INITRAMFS_MAXSIZE ?= "262144"
