[Unit]
Description=Data Partition
DefaultDependencies=no
Conflicts=umount.target
Before=local-fs.target
@PREPARE_DEPENDENCY@

[Mount]
What=@IMAGE_DATA_PART_DEVICE_NODE@
Where=/data
Type=ext4
Options=@IMAGE_DATA_PART_MOUNT_OPTIONS@

[Install]
WantedBy=local-fs.target
